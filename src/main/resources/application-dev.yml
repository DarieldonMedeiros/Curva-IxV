spring:
  datasource:
    url: JDBC:sqlite:./ivcurve-dev.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
logging:
  level:
    com.seuapp.ivcurve: DEBUG
